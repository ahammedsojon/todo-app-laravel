const apiVersion = "/api/v1.0";

const apiRoutes = {
    todoCreate: apiVersion + '/todo/create',
    todoUpdate: apiVersion + '/todo/update',
    todoList: apiVersion + '/todo/list',
    todoDelete: apiVersion + '/todo/delete',
}


export  default  apiRoutes;
